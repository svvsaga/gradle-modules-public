{
  "name": "gradle-modules",
  "description": "Common Gradle modules for SVV Saga projects",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "showLicenses": "cd modules && ./gradlew generateLicenseReport && open build/reports/dependency-license/index.html"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.10.2",
    "husky": "^7.0.2",
    "lint-staged": "^11.2.0",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
